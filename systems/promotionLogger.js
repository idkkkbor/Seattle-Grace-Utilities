const { EmbedBuilder } = require("discord.js");

function startPromotionLogger(client) {

    console.log("Promotion logger started.");

    // Promotion logger will be connected to the SGH
    // Maple Hospital promotion webhook here.

}

module.exports = {
    startPromotionLogger
};
